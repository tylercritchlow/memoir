[package]
name = "untitled"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace]

[dependencies]
memoir-logger = { version = "1.2.3", path = "src/memoir_logger", features = ["grouping"] }
